kind = "web"
previewPath = "/"
title = "Rice Disease Detector"
version = "1.0.0"
id = "artifacts/rice-detector"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25227

[services.development]
run = "pnpm --filter @workspace/rice-detector run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rice-detector", "run", "build" ]
publicDir = "artifacts/rice-detector/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25227"
BASE_PATH = "/"
